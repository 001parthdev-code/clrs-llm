Prompt: Red black tree

============================================================
Red black tree is a node is a descendant of the tree. The tree is connected by a pointer to the node. The nodes preceding the tree, shown in Figure 13.4(a). The tree is a rooted tree with root
7. (c) The same tree as the root of the tree with root
============================================================